export const SVG_CONTAINER_STYLE = {
  position: 'absolute',
  top: 0,
  right: 0,
  bottom: 0,
  left: 0,
} as const;

export const DEFAULT_SVG_PROPS = {
  width: '100%',
  height: '100%',
  preserveAspectRatio: 'none',
} as const;

export const DEFAULT_STROKE_COLOR = '#333';
export const DEFAULT_STROKE_WIDTH = 4;
